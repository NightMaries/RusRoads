import { DrawingGraph } from './drawingGraph';
export declare enum OrderingEnum {
    in = 0,
    out = 1
}
export declare enum DirTypeEnum {
    forward = 0,
    back = 1,
    both = 2,
    none = 3
}
export declare function parseDotString(graphStr: string): DrawingGraph;
