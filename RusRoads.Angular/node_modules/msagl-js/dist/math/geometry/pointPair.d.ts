import { Point } from '.';
/** An unordered pair of points */
export declare class PointPair {
    first: Point;
    second: Point;
    constructor(first: Point, second: Point);
    get First(): Point;
    get Second(): Point;
    get Length(): number;
    CompareTo(other: PointPair): number;
    static equal(pair0: PointPair, pair1: PointPair): boolean;
    toString(): string;
}
